
/// <reference no-default-lib="true"/>
/// <reference lib="ESNext" />
/// <reference lib="DOM" />
/// <reference types="@webgpu/types" />
/// <reference path="./index.d.ts" />

navigator.gpu

HTMLCanvasElement.prototype.addEventListener

LaunchParams

window.LaunchParams

launchQueue

// crypto = new Crypto();

window.crypto = new Crypto()

// new LaunchQueue();

new WindowControlsOverlayGeometryChangeEvent("geometrychange", {
	visible: true,
	titlebarAreaRect: new DOMRect(),
});

// new Event()

const div = document.createElement("div");
const a = div.setHTML("hi");

document.querySelector<HTMLElement>("asdf")!.style.colorScheme;

// document.addEventListener("load", async (event) => {
// 	if (!event.transition) return;
// 	document.querySelector(".header").style.pageTransitionTag = "header";

// 	await event.transition.ready;

// 	// The pseudo-elements are now accessible and can be animated:
// 	document.documentElement.animate(keyframes, {
// 		...animationOptions,
// 		pseudoElement: "::page-transition-container(header)",
// 	});
// });

new URLPattern();

